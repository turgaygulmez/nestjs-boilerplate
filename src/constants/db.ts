/**
 * Defines the DB provider name
 */
export const PGDB_PROVIDER_ROOT = 'PGSQL_SEQUELIZE';

/**
 * list to define each table repository
 */
export enum DB_PROVIDER {
  CLIENT = 'CLIENT',
}

/**
 * list for db table names
 */
export enum DB_TABLE {
  CLIENT = 'CLIENT',
}
